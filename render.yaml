# DEPRECATED: This file was for Render deployment
# DigitalOcean deployment config is now in .do/app.yaml
# 
# For reference, the old Render static site config was:
# - Build: npm ci && npm run build  
# - Publish: dist/
# - SPA rewrite: /* -> /index.html
# - Env vars: VITE_SUPABASE_URL, VITE_SUPABASE_PUBLISHABLE_KEY
